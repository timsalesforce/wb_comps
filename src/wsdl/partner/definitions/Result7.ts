import { AppMenuItems } from "./AppMenuItems";

/**
 * result
 * @targetNSAlias `tns`
 * @targetNamespace `urn:partner.soap.sforce.com`
 */
export interface Result7 {
    /** appMenuItems[] */
    appMenuItems?: Array<AppMenuItems>;
}
