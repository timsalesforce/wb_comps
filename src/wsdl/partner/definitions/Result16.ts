import { ApprovalLayouts } from "./ApprovalLayouts";

/**
 * result
 * @targetNSAlias `tns`
 * @targetNamespace `urn:partner.soap.sforce.com`
 */
export interface Result16 {
    /** approvalLayouts[] */
    approvalLayouts?: Array<ApprovalLayouts>;
}
