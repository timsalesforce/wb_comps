import { Columns2 } from "./Columns2";

/**
 * records
 * @targetNSAlias `tns`
 * @targetNamespace `urn:partner.soap.sforce.com`
 */
export interface Records {
    /** columns[] */
    columns?: Array<Columns2>;
}
